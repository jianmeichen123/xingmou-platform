<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.gi.xm.platform.dao.RoundDAO">
    <resultMap id="BaseResultMap" type="com.gi.xm.platform.pojo.Round">
        <id column="id" property="id" jdbcType="INTEGER"/>
        <result column="title" property="title" jdbcType="VARCHAR"/>
        <result column="code" property="code" jdbcType="VARCHAR"/>
        <result column="ordernum" property="ordernum" jdbcType="INTEGER"/>
        <result column="type" property="type" jdbcType="INTEGER"/>
        <result column="parent_id" property="parentId" jdbcType="BIGINT"/>
        <result column="add_time" property="addTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer">
        DELETE FROM dw_v_round
        WHERE id = #{id,jdbcType=INTEGER}
    </delete>

    <insert id="insert" parameterType="com.gi.xm.platform.pojo.Round" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO dw_v_round (
        title,
        code,
        ordernum,
        type,
        parent_id,
        add_time,
        update_time
        )
        VALUES
        (
        #{title,jdbcType=VARCHAR},
        #{code,jdbcType=VARCHAR},
        #{ordernum,jdbcType=INTEGER},
        #{type,jdbcType=INTEGER},
        #{parentId,jdbcType=BIGINT},
        #{addTime,jdbcType=TIMESTAMP},
        UNIX_TIMESTAMP()
        )
    </insert>

    <update id="updateByPrimaryKey" parameterType="com.gi.xm.platform.pojo.Round">
        UPDATE dw_v_round
        SET
        <if test="title!=null">title = #{title,jdbcType=VARCHAR},</if>
        <if test="code!=null">code = #{code,jdbcType=VARCHAR},</if>
        <if test="ordernum!=null">ordernum = #{ordernum,jdbcType=INTEGER},</if>
        <if test="type!=null">type = #{type,jdbcType=INTEGER},</if>
        <if test="parentId!=null">parent_id = #{parentId,jdbcType=BIGINT},</if>
        <if test="addTime!=null">add_time = #{addTime,jdbcType=TIMESTAMP},</if>
        WHERE id = #{id,jdbcType=INTEGER}
    </update>


    <sql id="selectFields">
        id,title,code,ordernum,type,parent_id,add_time,update_time
    </sql>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT
        <include refid="selectFields"/>
        FROM dw_v_round
    </select>

    <select id="queryRound" resultMap="BaseResultMap" parameterType="com.gi.xm.platform.query.RoundQuery">
        SELECT
        <include refid="selectFields"/>
        FROM dw_v_round
        <include refid="queryWhereAndOrder"/>
    </select>

    <sql id="queryWhereAndOrder">
        <where>
            <if test="id!=null">AND id = #{id,jdbcType=INTEGER}</if>
            <if test="title!=null and title!=''">AND title = #{title,jdbcType=VARCHAR}</if>
            <if test="code!=null and code!=''">AND code = #{code,jdbcType=VARCHAR}</if>
            <if test="ordernum!=null">AND ordernum = #{ordernum,jdbcType=INTEGER}</if>
            <if test="type!=null">AND type = #{type,jdbcType=INTEGER}</if>
            <if test="parentId!=null">AND parent_id = #{parentId,jdbcType=BIGINT}</if>
            <if test="addTime!=null">AND add_time = #{addTime,jdbcType=TIMESTAMP}</if>
            <if test="updateTime!=null">AND update_time = #{updateTime,jdbcType=TIMESTAMP}</if>

        </where>
        <choose>
            <when test="order!=null and orderBy!=null and order!='' and orderBy!=''">order by ${orderBy} ${order}</when>
            <otherwise>order by id desc</otherwise>
        </choose>
    </sql>

    <select id="selectByPrimaryKey" resultMap="BaseResultMap" parameterType="java.lang.Integer">
        SELECT
        <include refid="selectFields"/>
        FROM dw_v_round
        WHERE id = #{id,jdbcType=INTEGER}
    </select>

    <select id="selectByPrimaryKeyForUpdate" resultMap="BaseResultMap" parameterType="java.lang.Integer">
        SELECT
        <include refid="selectFields"/>
        FROM dw_v_round
        WHERE id = #{id,jdbcType=INTEGER}
        FOR UPDATE
    </select>

    <!--　根据类型和patentId查询 -->
    <select id="selectByTypeParentId" resultMap="BaseResultMap">
        SELECT
            <include refid="selectFields"/>
        FROM
            dw_v_round
        WHERE
            type = #{type,jdbcType=INTEGER}
        <if test="parentId != null">
            AND  parent_id = #{parentId,jdbcType=INTEGER}
        </if>
    </select>

</mapper>